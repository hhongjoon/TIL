# ==============데이터통신=============

### 데이터전송이론

아날로그 vs 디지털



비동기식에서 전송 없을 때 : 휴지상태 (idle)

### 신호변환방식 _ 디지털변조 _ 펄스코드변조

```
디지털변조 (keying) : 디지털신호 -> 아날로그 신호
- ASK (진폭 편이 변조)
- FSK (주파수 편이 변조)
- PSK (위상 편이 변조)
- QAM (직교 위상 변조)

============================================================
펄스코드변조(PCM)(Pulse Code Modulation), 코덱이용
아날로그 신호 -> 디질털 신호

- 펄스변조 : 펄스파의 진폭, 폭, 위상 등을 변화시키는 변조
- 아날로그 -> 디지털
--------------- 송신 ----------
- 표본화 : 최대 주파수 * 2 => PAM
- 양자화 : 양자화 계단 수 =  2^(비트수) : 대표값을 조정
- 부호화
--------------- 수신 --------
- 복호화
- 여과화

펄스변조순서 : 신호 - 표본화 - 양자화 - 부호화
PCM ->(표본화) -> PAM ->(양자화) -> PCM ->(부호화) -> PCM결과
```



#### 다중화 (multiplexing) : 하나의 고속통신회선을 다수의 단말기가 공유

```
- 다중화기 : 단말기들의 속도 합 => 회선의 속도

- 주파수 분할 다중화기(FDM : frequency division multiplexer)
  	라디오, 티비연상/가드밴드 존재
  	가드 밴드 존재
  	변,복조 기능 포함

- 시분할 다중화기
	- 동기식 시분할 다중화기(STDM) : 슬롯 낭비 존재
	- 비동기식 시분할 다중화기(ATDM)(지능 다중화기, 통계적) : 복잡
	
- 역다중화기 : 회선 여러개 -> 변경 쉬움

- 집중화기 (concentrator)

*** 다중화기는 하나의 통신회선을 여러 단말기
    집중화기는 하나 또는 소수의 통신회선
    
*** 다중화기 ->(변,복조/ 가드밴드) 주파수 분할 다중화기
```



#### 통신속도와 용량

```
변조속도 : 1초동안 몇개의 신호 변화

신호속도 : 1초동안 전송가능 비트 수
	변조속도 (baud) * 상태 변화수
	ex) 1600 baud * 쿼드 비트
	     1600 * 4 bps
	     
	8위상 2진폭 => 4비트
	
	펄스의 수 -> 비트수



전송속도 : 단위시간당 전송되는 데이터 양

베어러 속도: 동기문자, 상태 신호등 합한 것 (bps)

통신 용량 : 단위 시간당 전송 최대치

```

데이터통신 추가



### 3장 전송제어 방식

#### 전송 제어의 개념 : 데이터의 원활함 흐름을 위함

​	입.출력 제어 / 회선제어 / 동기제어 / 오류제어 / 흐름제어 



전송제어절차

- 순서중요**

```
데이터 통신 회선의 접속 : point to point / multi point   ----  전회선일 경우 불필요 단계
데이터 링크설정 : 
정보 메시지 전송
데이터 링크 종결
데이터 통신 회선의 절단

회선 : 물리적경로
링크 : 논리적경로
```



#### 데이터 링크 제어 프로토콜 (BSC)

```
SYN
SOH
STX
ETX
ETB
EOT
ENQ
ACK
NAK
```



#### 데이터 링크 제어 프로토콜(HDLC)

- HDLC (high level data link control)

```

```

- HDLC 프레임 구조

```
플래그    주소부    제어부   정보부    FCS   플래그


I 프레임 정보 0으로시작 : 사용자 데이터 전달
S 프레임 감독 10 시작 : 오류, 흐름 제어
U 프레임 비번호 11 시작 : 링크관리(설정, 해제)
```



- 데이저 전송 모드

```
표준 응답 모드 : 주국의 허가 있을때 송신

비동기 응답 모드 : 주국 허가없이도 송신

비동기 균형 모드 : 혼합국끼리 허가 없이 언제나 송신
```

​	피기백킹 : 데이터 프레임에 확인 응답을 포함시켜 전송



#### 오류 제어 방식

- 오류발생원인 : 전송과정에서 생긴기는 오류

```
감쇠
지연왜곡

잡음
	백색잡음
	상호 변조 잡음
	누화 잡음=혼선
	충격성 잡음
	돌발성 잡음
	위상 지터 잡음 : 연소적
	위상 히트 잡음 : 불연속적
	
===================================
우연적 왜곡 : 백색잡음, 충격잡음, 노화 잡음(혼선), 위상히트 잡음
시스템적 왜곡 : 손실, 감쇠, 하모닉 왜곡
	
	
```

- 전송오류 제어 방식

```
- 전진 오류 수정 (FEC) : 오류를 스스로 수정
	채널 필요 X
	수정을 위한 비트때문에 전송효율 떨어짐
	해밍코드. 상승코드 			- 검출과 수정 방법

- 후진 오류 수정 (BEC) : 송신측에서 재전송 요규
	패리티 검사, CRC, 블록합  - 오류검출
	자동반복요청(ARQ)        - 오류제어
	
ARQ(자동 반복 요청)
정지-대기 ARQ
연속 ARQ
적응적 ARQ

	
```



#### 오류 검출 방식

```
오류 검출 코드 이용

패리티검사
순환 중복검사 : 집단적으로 발생, 다항식, 동기전송, FCS사용
궤한 전송 방식
자동 연속 방식
해밍코드 방식
상승코드 방식
```



### 4장 데이터 회선망



#### 데이터 교환 방식 - 회선교환 : 두 지점을 물리적으로 교환

```
- 일단 접속이 되면 전용선처럼 사용한다.

##### SDS (공간 분할 교환 방식)

- 1단공간 분할 : 고장시 의존하는 두 장치가 연결X
- 다단 공간 분할 : 1단분할 보완,



##### TDS(시 분할 교환 방식)

- 데이터 전용 회선
  - TDM / 타일 슬롯 / 시간 다중화


```

- #### 회선교환 특징

```
수신이 순서대로 진행
에러제어, 복구가 사용자가 해야함 -> 복잡함
고정 대역폭(band width) 사용
에러 발생시 고치기 어려움 (사용자가 처리해야하기 때문)
```



#### 데이터 교환 방식 - 축적교환 : 기억장치 사용

```
- 메시지 교환 방식
	메시지단위 !!
	통신회선이 빌 때 메시지가 전송되므로 지연 시간이 길다.

- 패킷 교환 방식
	빠른 전송을 위해 메시지를 패킷단위!!!
	종류
	- 가상 회선 방식 : 논리적 가상 통신 회선, 순서대로
	- 데이터그램 방식 : 연결경로 설정 X, 순서 상관 X
==============  분류기준 : 연결 경로의 설정 여부 ==========	
	
패킷 교환망 (PSDN)
- 패킷 다중화
- 경로 제어
- 논리 채널
- 순서 제어
- 트래픽 제어
- 오류 제어

Call - 연결 - Clear
```



#### 경로제어와 트래픽 제어

```
* 경로제어 : 패킷전송을 위한 주요 기능
경로 설정요소
 - 성능 기준 / 경로 결정 시간과 장소 /
   /정보 발생지 / 경로 정보의 갱신 시간/

* 경로 설정 프로토콜
IGP(내부 게이트 프로토콜)
 - RIP : 현재 가장널리 사용 / 30초마다 알림 / 반응 없을 시 이상상태
 - OSPF : 대규모 네트워크

EGP(외부 게이트 프로토콜)
 - 게이트웨이 간 라우팅에 사용
 - AS (자율시스템)간의 라우팅 테이블 전달

BGP()
 - EGP 보완위해서 만들어짐


* 경로설정 방식
 - 고정 경로
 - 적응 경로
 - 범람 경로
 - 임의 경로
```



#### 트래픽 제어 _ 패킷의 양 조절

```
흐름 제어 : 패킷의 양이나 속도 조절
 - 정지-대기
 - 슬라이딩 윈도우 : 수신 확인신호 (ACK, NAK)로 윈도우의 크기 걸졍
 					한번에 여러 프레임일 때 효율적임
```



#### 통신망

```
LAN

MAC (매체 접근 제어)
	CSMA

VAN (부가 가치 통신망) : 통신 사업자가 서비스용으로 개설
```



#### 인터넷 주소체계

```
5클래서 존재 ( A B C D E)

서브네팅
 - 할당된 네트워크 주소를 여러개의 네트워크로 나눔
 
네트워크 - 호스트 주소 나눔 : 서브넷 마스크

IPv6 주소
 - IPv4의 주소 부족 문제를 해결하기 위함.
 - 128 비트 사용 ( 16비트 * 8부분)

IPv4 -> IPv6
 - 듀얼스택 : 두개의 스택 구성
 - 터널링 : IPv4에 터널을 만들어 IPv6 패킷이 통과하게끔 함.
```



### ===========  5장 통신 프로토콜  ==============

#### 통신 프로토콜의 개념 : 정보 전송의 통신규약

```
기본요소
	- 구문(Syntax)
	- 의미(Semantics)
	- 시간(Timing)
```



#### X.25 : 두 단말장치가 패킷 교환망을 통해 전달하기 위한 통신절차

```
DTE
DCE
계층구조
 - 물리 계층
 - 프레임 계층
 - 패킷 계층
```



#### TCP/IP 

#### 인터넷에 연결된 서로 다른 기종의 컴퓨터가 데이터 주고받는 프로토콜

```
TCP : OSL의 전송계층
 연결형 서비스 제공

IP : 네트워크 계층
 비연결형
 
```



##### TCP/IP 4계층

```
응용 계층
전송 계층
 - TCP : 양방향, 스트림
 - UDP
 - RTCP
인터넷 계층
네트워크 계층
```

#### TCP

```
스트림 위주 전송 서비스(전송 계층)
```

#### IP

```
비연결형 전송 서비스 제공 => 데이터그램 전송 지원 (인터넷 계층)
비신뢰성

```





#### ARP <-> RARP

```
ARP(주소 분석) : 논리주소 -> 물리주소
```





































